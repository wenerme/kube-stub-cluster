apiVersion: v1
kind: Service
metadata:
  name: mihomo-node-port
  labels:
    app: mihomo
  annotations:
    service.kubernetes.io/topology-mode: Auto
spec:
  selector:
    app: mihomo
  type: NodePort
  ports:
  - port: 7890
    targetPort: proxy
    name: proxy
    nodePort: 30789
