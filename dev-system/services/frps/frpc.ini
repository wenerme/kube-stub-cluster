[common]
server_addr = frp.example.com
token = {{.Envs.frps_token}}
server_port = 80
protocol = websocket

use_encryption = true
use_compression = true

[http]
type = http
subdomain = wener.frp.example.com
bind_addr = 0.0.0.0
bind_port = 3000
