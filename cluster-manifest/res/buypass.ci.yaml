apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: buypass
spec:
  acme:
    server: https://api.buypass.com/acme/directory
    privateKeySecretRef:
      name: buypass-account
    solvers:
    - http01:
        ingress: { }
