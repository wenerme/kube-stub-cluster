apiVersion: v1
kind: Service
metadata:
  name: {{ include "minio-console.fullname" . }}
  labels:
    {{- include "minio-console.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minio-console.selectorLabels" . | nindent 4 }}
